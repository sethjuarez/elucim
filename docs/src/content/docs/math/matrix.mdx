---
title: Matrix
description: Render matrices with bracket notation for linear algebra visualizations.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import LiveDemo from '@components/LiveDemo.tsx';
import { MatrixDemo } from '@components/Demos.tsx';

The `<Matrix>` primitive renders a two-dimensional array of values surrounded by square brackets, perfect for linear algebra and systems-of-equations scenes.

<LiveDemo client:visible height="300px">
  <MatrixDemo client:visible />
</LiveDemo>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `values` | `(number \| string)[][]` | — | 2D array of cell values |
| `x` | `number` | `0` | X position of the matrix |
| `y` | `number` | `0` | Y position of the matrix |
| `cellSize` | `number` | `40` | Width and height of each cell in pixels |
| `color` | `string` | `'currentColor'` | Text color for cell values |
| `bracketColor` | `string` | `'currentColor'` | Color of the surrounding brackets |
| `fontSize` | `number` | `16` | Font size of cell values |
| `fadeIn` | `boolean` | `false` | Animate the matrix in on mount |

## Code Example

<Tabs>
  <TabItem label="React">
    ```jsx
    import { Player, FadeIn, Matrix } from '@elucim/core';

    <Player width={500} height={300} fps={30} durationInFrames={60}>
      <FadeIn duration={20}>
        <Matrix
          values={[[1, 0, 0], [0, 'cos θ', '-sin θ'], [0, 'sin θ', 'cos θ']]}
          x={250} y={150} cellSize={55}
          color="currentColor" bracketColor="#6c5ce7"
        />
      </FadeIn>
    </Player>
    ```
  </TabItem>
  <TabItem label="DSL (JSON)">
    ```json
    {
      "width": 500, "height": 300, "fps": 30, "durationInFrames": 60,
      "children": [
        {
          "type": "matrix",
          "values": [[1, 0, 0], [0, "cos θ", "-sin θ"], [0, "sin θ", "cos θ"]],
          "x": 250, "y": 150, "cellSize": 55,
          "bracketColor": "#6c5ce7",
          "fadeIn": { "duration": 20 }
        }
      ]
    }
    ```
  </TabItem>
</Tabs>

:::tip
Cell values can be strings — use `"a₁₁"` or `"…"` for symbolic or abbreviated entries.
:::

