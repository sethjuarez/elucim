name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      package:
        description: Package to publish manually
        required: true
        default: cli
        type: choice
        options:
          - cli

permissions:
  contents: write
  pull-requests: write
  id-token: write
  packages: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      releases_created: ${{ steps.release.outputs.releases_created }}
      core_release: ${{ steps.release.outputs['packages/core--release_created'] }}
      dsl_release: ${{ steps.release.outputs['packages/dsl--release_created'] }}
      editor_release: ${{ steps.release.outputs['packages/editor--release_created'] }}
      cli_release: ${{ steps.release.outputs['packages/cli--release_created'] }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  publish:
    needs: release-please
    if: ${{ needs.release-please.outputs.releases_created == 'true' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: pnpm test
      - run: pnpm test:package-smoke

      - name: Publish @elucim/core
        if: ${{ needs.release-please.outputs.core_release == 'true' }}
        run: pnpm --filter @elucim/core publish --access public --no-git-checks --provenance

      - name: Publish @elucim/dsl
        if: ${{ needs.release-please.outputs.dsl_release == 'true' }}
        run: pnpm --filter @elucim/dsl publish --access public --no-git-checks --provenance

      - name: Publish @elucim/editor
        if: ${{ needs.release-please.outputs.editor_release == 'true' }}
        run: pnpm --filter @elucim/editor publish --access public --no-git-checks --provenance

      - name: Publish @elucim/cli
        if: ${{ needs.release-please.outputs.cli_release == 'true' || (github.event_name == 'workflow_dispatch' && inputs.package == 'cli') }}
        run: pnpm --filter @elucim/cli publish --access public --no-git-checks --provenance

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://npm.pkg.github.com
          scope: '@sethjuarez'

      - name: Publish @sethjuarez/elucim-core to GitHub Packages
        if: ${{ needs.release-please.outputs.core_release == 'true' }}
        run: node scripts/publish-github-package.mjs @elucim/core
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish @sethjuarez/elucim-dsl to GitHub Packages
        if: ${{ needs.release-please.outputs.dsl_release == 'true' }}
        run: node scripts/publish-github-package.mjs @elucim/dsl
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish @sethjuarez/elucim-editor to GitHub Packages
        if: ${{ needs.release-please.outputs.editor_release == 'true' }}
        run: node scripts/publish-github-package.mjs @elucim/editor
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish @sethjuarez/elucim-cli to GitHub Packages
        if: ${{ needs.release-please.outputs.cli_release == 'true' || (github.event_name == 'workflow_dispatch' && inputs.package == 'cli') }}
        run: node scripts/publish-github-package.mjs @elucim/cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
