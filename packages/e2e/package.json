{
  "name": "@elucim/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "playwright test --reporter=line",
    "test:workflows": "playwright test tests/core-workflows.spec.ts tests/state-machine.spec.ts --reporter=line",
    "test:all": "playwright test --reporter=line",
    "test:line": "playwright test --reporter=line",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^24.12.3",
    "typescript": "^5.5.0"
  }
}
