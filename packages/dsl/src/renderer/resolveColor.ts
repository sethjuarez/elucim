/**
 * Semantic color token resolution for elucim DSL.
 *
 * Re-exports the canonical implementation from @elucim/core.
 * This file is kept for backward compatibility — all new code should
 * import directly from `@elucim/core`.
 */
export { resolveColor, SEMANTIC_TOKENS, TOKEN_NAMES } from '@elucim/core';

